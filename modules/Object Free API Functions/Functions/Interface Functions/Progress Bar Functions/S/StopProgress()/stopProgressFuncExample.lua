--* StopProgress() Function - Example

--* About
    -- This example stops the progress bar created using the StartProgress example.

--* Example 1
return function()
    -- Stops and closes the progress bar with the matching handle
    StopProgress(progressHandle)
end