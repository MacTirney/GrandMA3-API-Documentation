--* GetUIChannelCount() Function - Example

--* About
    -- This example prints the amount of UI channels to the Command Line History.

--* Example 1
return function()

    Printf("The number of UI channels is " .. GetUIChannelCount())
    
end