--* GetAttributeCount() Function - Example

--* About
    -- This example prints the returned number in the Command Line History.

--* Example 1
return function()

    Printf("The attribute count is %i", GetAttributeCount())
    
end